# Running


```
$ bokeh serve --allow-websocket-origin=127.0.0.1:5000 . &
$ python client.py
```
Be sure client.py flask app is running on port 5000, and it should load just fine.

# Layout dimensions

settings panel: 500px

